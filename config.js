// ─────────────────────────────────────────────────────────────
// JL Customs kiosk — central configuration
//
// Change the backend URL here ONCE. Every page loads this file in
// its <head> before its own scripts run, so this is the single
// source of truth for where the kiosk talks to the server.
// ─────────────────────────────────────────────────────────────
window.JLConfig = {
  serverUrl: 'https://interactive-monitor-thing.onrender.com',
};
